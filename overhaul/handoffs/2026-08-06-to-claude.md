# Handoff to Claude — 2026-08-06

Copy this into the next Claude session:

---

You are taking over review/orchestration for the TutorPlatform overhaul after Codex's first Wave 1 implementation run.

Read `AGENTS.md`, then `overhaul/progress-log.md`, `overhaul/tasks.json`, `overhaul/08-agent-task-packets.md`, `overhaul/16-defaulted-decision-packets.md`, and `overhaul/17-claude-contracts.md`. Do not re-derive completed audit findings.

Repository state:

- Local `main` is clean at `5a230d2` and is 7 commits ahead of `origin/main` (`51b48a1`). Those seven local commits contain the Wave 0 plan/test baseline, your decision packets/contracts, and Codex's progress-log update. Do not reset or overwrite them.
- GitHub branch pushes work, but PR creation is globally blocked: the GitHub integration returns `403 Resource not accessible by integration`, and `gh` is not installed. Consequently, no branch is formally `awaiting-review` and nothing has been merged.
- T0-01 is still partial: the required 28 Playwright baseline screenshots were not captured. This blocks T2-01 and Wave 2+ UI tasks.

Codex pushed these Wave 1 branches:

| Task | Branch | Commit | State |
|---|---|---:|---|
| T1-01/T1-02 | `overhaul/booking-seat-lock` | `ac74607` | Paired Type E; scoped implementation validated, acceptance incomplete |
| T1-03 | `overhaul/centers-gate-fix` | `dde4191` | Type B; proxy fix validated, page-level redirect remains |
| T1-05 | `overhaul/dashboard-revenue-gate` | `a6b2923` | Paired Type E; static/data-layer gates validated, runtime role fixtures missing |
| T1-06 | `overhaul/flutter-mock-data-fix` | `9ec9c01` | Type B; analyze/tests/build pass, live-device screenshot missing |
| T1-07/T1-08 | `overhaul/dep-upgrade-next` | `67593db` | T1-07 checks green; T1-08 credentialed auth smoke incomplete |
| T1-09 | `overhaul/login-open-redirect-fix` | `ca06514` | Type B; checks green |
| T1-11 | `overhaul/perf-suspense-navbar` | `f43e72c` | Paired Type E; streaming works, static/ISR acceptance fails |

Important review findings:

1. `ac74607` puts class lookup, duplicate detection, active-seat count, booking write, and promo validation/increment in one serializable transaction, retries `P2034`, and maps conflicts to clean `409` responses. It deletes the dead `lockSeat()` implementation. Cash bookings are `CONFIRMED + UNPAID`; free bookings alone are immediately paid. It restores localized honest checkout and the existing promo component, and replaces fabricated Paymob identity values with authenticated profile values. Unit tests, focused lint, TypeScript, production build, and independent Type E source review passed.
2. T1-01/T1-02 cannot be accepted as currently scoped. The registry excludes `app/classes/[id]/book/page.tsx`, which is needed to supply structured real dates and session profile defaults, and excludes `app/api/cron/cleanup-locks/route.ts`, which is needed for cron acceptance. There is also an out-of-scope late-Paymob-webhook/reallocated-seat race and abandoned online payments can consume promo usage.
3. `dde4191` correctly removes the broad proxy role restriction while preserving `/centers/[id]/admin`, but `app/centers/page.tsx` independently redirects anonymous users. The task cannot meet anonymous acceptance with `proxy.ts` alone.
4. `a6b2923` prevents restricted center tutors from receiving financial/PII queries and gates payouts/analytics before financial access. Acceptance-grade FULL/LIMITED/VIEW_ONLY accounts and captured network payloads are unavailable.
5. `67593db` upgrades to `next@16.3.0`, `next-auth@5.0.0-beta.32`, and `@auth/core@0.41.3`; audit/test/lint/tsc/build pass. The GitHub advisory confirms beta.32 patches SEC-001. Anonymous session/provider smoke passed, but login, OAuth linking, five-attempt lockout, and session invalidation need safe credentials. The configured Google client is a placeholder.
6. `f43e72c` adds the requested Suspense boundary and an inert token-matched Navbar skeleton. Test/lint/tsc/build plus English/Arabic desktop/mobile browser checks passed. The complete 117-route table remains entirely `ƒ`; PERF-001 is not closed. Static/ISR conversion requires Cache Components/configuration and broader route-data work outside `app/layout.tsx` and `app/Navbar.tsx`.
7. T1-10 was not edited. Its acceptance requires changing `app/admin/AdminClient.tsx`, but `tasks.json` authorizes only the approval API route and `lib/paymob.ts`, while the packet forbids automation. Fix the registry/packet mismatch before handing it back.

What Claude should do next:

1. Restore a PR-capable GitHub credential or tell the owner to open PRs from the pushed branches. Do not mark branches `awaiting-review` without real PRs.
2. Perform the required review pass(es), especially the two-pass Type E review for T1-01/T1-02, T1-05, T1-08, and T1-11.
3. Resolve the filesAffected mismatches for T1-01/T1-02, T1-03, T1-10, and T1-11 by either issuing explicit amended packets or narrowing acceptance. Codex deliberately made no out-of-scope edits.
4. Provide safe seeded accounts/data for concurrency, cron, authorization-payload, and auth-flow evidence.
5. Finish T0-01's screenshot baseline. Do not open Wave 2 UI work before both the baseline and Wave 1 exit gates are satisfied.
6. Reconcile the default-decision packets with the still-populated `blockedBy` fields for T1-04, T4-01, and T4-05 so the next Codex session has one unambiguous instruction source.

The authoritative event trail is `overhaul/progress-log.md`.

---
