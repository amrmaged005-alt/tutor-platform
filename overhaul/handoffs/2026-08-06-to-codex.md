# Handoff to the Next Codex Session — 2026-08-06

Copy this into the next Codex session:

---

Continue the TutorPlatform overhaul from the existing repository state. Do not restart the audit or redo pushed work.

First read, in order:

1. `AGENTS.md`
2. `overhaul/14-overhaul-index.md`
3. `overhaul/tasks.json`
4. `overhaul/08-agent-task-packets.md`
5. `overhaul/07-shared-foundations-and-file-ownership.md`
6. `overhaul/progress-log.md`
7. `overhaul/16-defaulted-decision-packets.md`
8. `overhaul/17-claude-contracts.md`
9. This handoff

Starting state:

- Local `main` is clean at `5a230d2`, seven commits ahead of `origin/main` (`51b48a1`). Preserve those local commits.
- Branch pushes work. GitHub PR creation does not: the connector returns `403 Resource not accessible by integration`, and `gh` is absent.
- No Codex branch is merged or formally `awaiting-review`.
- T0-01 remains partial because its 28 Playwright screenshots are missing. Wave 2+ UI work is not ready.
- Use the existing worktrees under `C:\Users\Amr\tutor-platform-worktrees`; do not create duplicate worktrees for the same branches.

Pushed branches:

- `overhaul/booking-seat-lock` — `ac74607` — T1-01/T1-02
- `overhaul/centers-gate-fix` — `dde4191` — T1-03
- `overhaul/dashboard-revenue-gate` — `a6b2923` — T1-05
- `overhaul/flutter-mock-data-fix` — `9ec9c01` — T1-06
- `overhaul/dep-upgrade-next` — `67593db` — T1-07/T1-08
- `overhaul/login-open-redirect-fix` — `ca06514` — T1-09
- `overhaul/perf-suspense-navbar` — `f43e72c` — T1-11

Do these checks before editing:

1. Read the latest `overhaul/progress-log.md` to detect activity from another session.
2. Check whether PR access has been restored and whether any pushed branch already has a PR or was merged.
3. Fetch/re-pull only after confirming it will not overwrite local `main`'s seven unpushed commits.
4. Re-evaluate wave readiness. Do not advance merely because implementation branches exist; required reviews/merges and T0-01 evidence are still missing.

Immediate work queue:

1. If PR access is restored, open PRs against `main` for every pushed branch with the structured handoff from `overhaul/09-review-validation-and-handoff.md`. Mark paired/Type E PRs `AWAITING CLAUDE REVIEW`; do not merge them.
2. For green Type B branches T1-03, T1-06, T1-07, and T1-09, merge only through the authorized PR workflow and only when each acceptance criterion is actually evidenced. T1-03 and T1-06 currently have evidence gaps.
3. Do not widen T1-01/T1-02. It still needs explicit authority for `app/classes/[id]/book/page.tsx` and `app/api/cron/cleanup-locks/route.ts`, safe seeded concurrent/cron tests, and a decision on the late-webhook/promo-reservation risks.
4. Do not widen T1-03 into `app/centers/page.tsx` without an amended packet.
5. Do not implement T1-10 until `tasks.json.filesAffected` includes its actual UI file, `app/admin/AdminClient.tsx`, or Claude issues an explicit authoritative correction.
6. Do not claim T1-11 complete. The full after-build table still has 117 dynamic routes; the branch improves streaming only. Cache Components/configuration is outside its current protected-file scope.
7. T1-05 needs real FULL/LIMITED/VIEW_ONLY fixtures and captured response payloads.
8. T1-06 needs a configured Flutter device/backend and a real-data screenshot.
9. T1-08 needs safe credentialed login, OAuth linking, lockout, and session-invalidation tests. Do not test mutations against the remote database without approved disposable fixtures.
10. Resolve the contradiction between `overhaul/16-defaulted-decision-packets.md` and the canonical `blockedBy` fields for T1-04, T4-01, and T4-05 before implementing those tasks.

Validation already completed:

- All seven branches were pushed successfully.
- Applicable unit tests, lint, TypeScript, and builds passed.
- T1-01/T1-02 received an independent source review; its core serializable transaction is sound after cash-payment and legacy-lock corrections.
- Auth.js beta.32 was verified against the official SEC-001 advisory.
- T1-11 was checked in English and Arabic at desktop/mobile sizes, but static/ISR acceptance failed.

Stop condition: continue only through tasks that are genuinely ready under the dependency graph and wave entry criteria. If PR authorization, fixtures, or filesAffected authority remain missing, update the progress log with concrete evidence and hand back the exact blocker—do not fabricate completion or silently cross scope.

---
